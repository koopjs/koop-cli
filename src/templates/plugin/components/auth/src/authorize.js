module.exports = async (req) => {
  // Verify the request is being made by an authenticated user
  // (e.g., validate a token in the authorization header)

  // throw a 401 error if not authorized
  // const error = new Error('Not Authorized')
  // error.code = 401
  // throw error

  // resolve the promise if successful.
  return {}
}
